# Neon's DIRECT connection string (no "-pooler" suffix), not the pooled one —
# drizzle-kit generate/migrate need a direct connection. createDb() in src/client.ts
# is the pooled-string consumer; see ADR 0012.
DATABASE_URL=
