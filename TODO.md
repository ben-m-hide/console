# TODO

Task tracking for console-next, following the [todo-md](https://github.com/todo-md/todo-md) standard: `- [ ]` open, `- [x]` done, `- [-]` declined. See `docs/adr/` and `README.md` for the reasoning behind any item — this file is the action list, not the rationale. Claude Code tooling/meta tasks (skills, settings, hooks) are tracked separately in [`.claude/TODO.md`](.claude/TODO.md) — this file is product/repo tasks only.

## Backend

- [ ] **High priority.** Migrate `players/compare-route.ts`'s hand-rolled `innerJoin` chain to `packages/db`'s new relational-query API (`db.query.playerSeasonStats.findMany({ with: {...} })`) once the relation graph lands (see `docs/plans/2026-08-19-api-architecture-review.md` Part 2) — deferred out of that same change deliberately (touching tested/working code purely for structural consistency isn't a surgical change), but the new API needs to prove itself on this route, not just on whatever nested route builds it next.
- [x] Wire up a test setup for `apps/api` once there's real behavior worth testing (`node` environment, not `apps/web`'s `jsdom`) — done 2026-08-14, `vitest.config.ts` mirroring `apps/ingestion`/`packages/shared`. Same split as ingestion's `normalize-*.ts`/`ingest-*.ts`: extracted `players-compare.ts`'s percentile calc/transfer-dedup/missing-player logic into a pure `build-player-compare-entries.ts` (unit tested, no DB import), `error-handler.ts`'s three exports tested via Hono's own `app.request()` pattern. Both routes' actual `db.select()` calls deliberately not unit tested — same reasoning as `ingest-*.ts` orchestrators, avoids a DB-mocking abstraction and automated tests silently hitting the real `local-dev` branch. 13 tests, 0 `DATABASE_URL` needed. Verified the extraction is behavior-preserving: re-ran the same real request against real data before/after, byte-identical output. See `docs/plans/2026-08-14-api-vitest-setup.md`.
- [ ] Migrate backend compute from Render to AWS Lambda/Fargate + EventBridge once an AWS account is confirmed and approved (see ADR 0010) — update `connect-src` again at that point. **Check the live-updates transport item below before committing to Lambda** — the two decisions are coupled.
- [ ] Investigate and decide a real-time/live-match update transport (WebSockets vs SSE vs polling), raised 2026-08-15 — **not now, deliberately**, but worth deciding before the compute migration above rather than after. Current state: PROJECT.md §3 already decided to skip live match tracking, and the whole ingestion design follows from that (scheduled job every few hours, rolling-window re-fetch). Three things would have to change first: (1) **no live data source** — Sportmonks' only near-real-time primitive is `GET /fixtures/latest` (10-second window, PROJECT.md §3/Phase 3), and this project's Starter-tier token has already hit subscription gates on other endpoints, so a live feed is plausibly a paid-tier upgrade — unverified, needs checking against the real plan matrix, not assumed; (2) **nothing to stream** — `match_events` still has no ingestion code at all, so a socket would be a pipe with no source; (3) **it conflicts with ADR 0010's Lambda target** — Lambda cannot hold long-lived connections, so live updates would force API Gateway WebSocket API (different programming model, needs a connection-state store) or Fargate instead. That makes "might we want live updates later?" an _input_ to the compute decision, not independent of it. When picked up, evaluate **SSE first, not WebSockets**: live match updates are one-way server→client, which is exactly SSE's shape (plain HTTP, built-in auto-reconnect, no separate protocol upgrade); bidirectional messaging has no identified use case here. Also weigh plain polling honestly — at a football match's actual event rate, a few-second poll may be indistinguishable to a user and vastly simpler to host.

## Football analytics domain (see `PROJECT.md`, ADR 0010)

- [x] Confirm the football-analytics domain as the backend's first real feature (schema, ingestion job, API surface) rather than leaving it open per ADR 0008 — API surface confirmed 2026-08-14 alongside the ingestion job: `apps/api` now has its first real route, `GET /api/v1/competitions`, backed by `packages/db`'s `competitions` table via `@console-next/db`/`@console-next/shared`. Also added `hono/cors` (origin from a new `API_CORS_ORIGIN` placeholder env var — no real deployed frontend origin exists yet, same situation as the CSP's `connect-src`) and an `app.onError` handler returning PROJECT.md §9's `{ error: { code, message } }` envelope. Every other route (`/players/*`, `/teams/*`, `/fixtures/*`) still needs tables that are still empty (fixtures/teams/players/match_events/ball_positions) — deliberately not built yet, would just return `[]` forever. `/health` moved to `/api/v1/health` per PROJECT.md §4's versioning decision (`/doc`/`/reference` stayed unversioned, dev tooling not part of the documented API surface); `run-api`'s `smoke.sh`/`SKILL.md` updated to match. Verified against the real `local-dev` Neon branch (5 real competitions returned), plus CORS-origin and 404 probes — found the 404 fallback initially still returned Hono's plain-text default, not the new envelope (`app.onError` only fires on thrown errors); fixed same-day with `app.notFound()`, verified the unmatched-route body now matches the envelope too.
- [ ] Decide ingestion deploy shape — same Render image/different entrypoint vs. a separate service — deliberately deferred to build time (PROJECT.md §10), not decided now
- [x] Define the `/players/compare` percentile baseline explicitly when built: same position + competition + season, minimum 450 minutes played (PROJECT.md §4) — done 2026-08-14 alongside the real route (`GET /api/v1/players/compare?ids=&season=`): peer group = `player_season_stats` joined to `players` (same `position`), filtered to the requested `seasonId` (a season belongs to exactly one competition, so no separate competition filter needed) and `minutesPlayed >= 450`. Backed by real `players`/`player_season_stats` ingestion, scoped to Premier League's most-recently-**finished** season only (not "current" — 2026/2027 hasn't started, ~zero real minutes played yet; see the ingestion entry below) — 698 real players, 714 real stat rows. Verified against real data: a 22-goal striker (Igor Thiago, Brentford) ranked 94.7th percentile among 113 qualifying attackers; a 15-goal midfielder (Morgan Gibbs-White) ranked 98.3rd among 117 midfielders — both plausible. Found and fixed a second envelope gap while probing: `@hono/zod-openapi`'s request validation responds directly (never throws), so it bypassed `app.onError` and returned its own raw `{success:false,error:ZodError}` shape instead of PROJECT.md §9's envelope — fixed via `OpenAPIHono`'s `defaultHook` option (`validationErrorHook` in `error-handler.ts`), verified malformed `ids`/missing `season` now return the same `{error:{code,message}}` shape as every other error path. `xa`/`xaPer90` (Expected Assists) dropped from `player_season_stats` before this landed — see the ingestion entry below.
- [ ] Don't display team logos or player photos — Sportmonks' terms require separately-sourced rights for those; use text/initials/placeholder icons instead (PROJECT.md §9)
- [x] Add the Sportmonks token to `.env.example` once `apps/ingestion` has real Sportmonks integration — done 2026-08-14, `apps/ingestion/.env.example`. Named `SPORTMONKS_TOKEN`, not `SPORTMONKS_API_KEY` as this item originally said — matches the var name already established in root `.env` from the Phase 3 trial work, no reason to introduce a second name for the same thing.
- [x] Add basic rate limiting on public API endpoints (e.g. `hono-rate-limiter`) once real routes exist (PROJECT.md §9) — done 2026-08-14, `hono-rate-limiter` (real npm package research: 590k weekly downloads, 0 deps, actively maintained), 100 requests/15min per `x-forwarded-for` IP, in-memory store (no Render service deployed yet — see the Deployment section — so a shared store like Redis would be premature). Applied to `/api/v1/competitions` and `/api/v1/players/*`, deliberately **not** `/api/v1/health` (uptime/monitoring checks shouldn't be throttled). Custom `handler` returns PROJECT.md §9's `{ error: { code, message } }` envelope instead of the library's own default response shape. Verified live: exactly 100 requests from one IP succeed, the 101st returns `429` with the correct envelope; `/api/v1/health` unaffected even from a hammered IP; a different IP gets through cleanly; `/api/v1/players/compare` independently rate-limited too (separate middleware mount, not accidentally sharing one bucket across routes).
- [x] Confirm whether Sportmonks exposes a delta/"updated since" fixture-fetch endpoint before building the ingestion job — already resolved in Phase 3 (2026-08-12, `PROJECT.md`'s own Phase 3 checklist): `GET /fixtures/latest` only covers the last 10 seconds, not usable at this project's cadence. Rolling-window re-fetch is the actual approach, not a fallback. This line was a stale duplicate of that finding, never marked done here.
- [x] Run `packages/db`'s generated migration once a Neon project exists — done 2026-08-14: Neon project created, a `local-dev` branch (PROJECT.md §7) holds local dev data, `bun run db:migrate` applied `drizzle/0000_*.sql` successfully, verified against `information_schema.tables` (all 10 tables present). Needed adding `postgres` (postgres.js) as a `packages/db` devDependency — `drizzle-kit`'s CLI needs a real driver package installed to actually connect, independent of the app's own `drizzle-orm/bun-sql` runtime driver (ADR 0012 unaffected, that decision was about runtime code, not CLI tooling).
- [x] Confirm the pooled connection string works end-to-end, and decide on cold-start tolerance — done 2026-08-14: `createDb()` against the pooled string ran a real query successfully (192ms first query, ~11ms warm). Decided **no app-level retry wrapper** — Bun's `SQL` client already retries a cold Neon compute with backoff up to its 30s default `connectionTimeout` (confirmed via the installed `bun-types` package, not docs prose), well past Neon's own ~few-hundred-ms typical wake time; see ADR 0012's "Consequences" and the comment in `packages/db/src/client.ts`. Revisit only if `apps/ingestion` shows real timeouts in practice.
- [x] Add `DATABASE_URL` to `apps/ingestion/.env.example` — done 2026-08-14, alongside the real Sportmonks integration below (pooled string, per ADR 0012).
- [x] Add `DATABASE_URL` to `apps/api/.env.example` once `createDb()` is actually wired in — done 2026-08-14, alongside the real `/api/v1/competitions` route above (pooled string, per ADR 0012). `.env.example` also gained `API_CORS_ORIGIN` for the new CORS middleware.
- [ ] Add `CHECK` constraints to `match_events` scoping `outcome`/`body_part`/`situation` by `type`, once Phase 4's Sportmonks API client surfaces the real `type`/`sub_type_id` values (deliberately not guessed — see ADR 0012, PROJECT.md §11 Phase 2)
- [x] Do a thin vertical slice before further schema/API elaboration — done 2026-08-14: `apps/ingestion` now has a real Sportmonks client (`competitions`/`League` entity only — `sportmonks-client.ts`, `normalize-competition.ts`, `ingest-competitions.ts`), run manually against the real API and Neon's `local-dev` branch. 5 leagues fetched and upserted, 0 failures; verified idempotent (ran twice, still 5 rows) and that a bad token fails loudly (401, not silent). Found and fixed a live Phase 1 assumption error along the way — `competitions.tier` had no Sportmonks data source and no downstream consumer, dropped (see `PROJECT.md`'s Phase 4 correction note). Fixtures/seasons/teams/match_events/ball_positions, rate limiting, `ingestion_runs` audit logging, and scheduling are still open — deliberately deferred, this was the "one real row" bar, not full Phase 4.
- [x] Ingest `teams` and `fixtures` — done 2026-08-14, scoped to each already-ingested competition's **current season only** (5 seasons), not a full historical backfill (see the new BACKLOG item below for that). Real API research corrected PROJECT.md §3's original plan along the way: the documented rolling-window `/fixtures/between/...` approach turned out to need a `fixtureLeagues`/`fixtureSeasons` filter this token's Starter subscription doesn't have access to (verified live — "no access via your current subscription"); `GET /seasons/{id}?include=fixtures.participants;fixtures.scores;fixtures.state` sidesteps that gate entirely and returns a season's full fixture list — including embedded team objects — in one request, an even stronger form of §3's "combine via `include=`" than a separate `/teams/seasons/{id}` call. Score extraction verified against `/core/types`: `type_id` 1525 is `CURRENT` (the live/final tally), not guessed from field shape alone. `apps/ingestion/src/index.ts` now ingests teams from the fixtures' embedded `participants` (no separate teams fetch), then fixtures themselves, resolving `seasonId`/`homeTeamId`/`awayTeamId` via `Map`s from already-ingested rows (same pattern as `ingest-seasons.ts`'s `competitionId`). Run twice against the real API + `local-dev` branch: 134/149 teams and 1,163/1,178 fixtures upserted both times (15 of each failed, cascading from the same root cause — 15 minor-club friendly opponents Sportmonks has no `short_code` for, so those teams don't upsert and their fixtures correctly fail FK resolution rather than writing a broken row). 83 fixtures already have a real final score recorded correctly.
- [x] Ingest `players` and `player_season_stats` — done 2026-08-14, scoped to **Premier League only**, its most-recently-**finished** season (not "current" — 2026/2027 hasn't started, ~zero real minutes played this early; see the ingestion detail above and the new BACKLOG item for the other 4 competitions). ~500 real Sportmonks requests for this one competition (a team's squad list, then one `/players/{id}?include=nationality;position;statistics.details.type` call per player — profile and every season's stats in one request), a real step up in volume from teams/fixtures (~20 requests) — same "one real slice before scaling" precedent. Found live that this subscription has no "Expected Assists" stat type anywhere (searched an entire finished-season squad, 32 players) — dropped `xa`/`xaPer90` from `player_season_stats` via a real migration (table had 0 rows, no data loss) rather than guess or leave permanently-null `NOT NULL` columns; same reasoning as `competitions.tier`'s earlier removal. Run against the real API + `local-dev` branch: 20 teams, 698/718 players, 714/734 stats upserted (20 failures cascade from academy/youth-squad players Sportmonks has no `nationality` for). See `docs/plans/2026-08-14-players-compare-route.md` for the full plan.

## Deployment

- [ ] Get/confirm an AWS account to deploy into
- [ ] `cdk bootstrap` + first real deploy of the hosting stack (see ADR 0007)
- [ ] OIDC-federated IAM role for GitHub Actions (no long-lived keys) + a CI deploy job
- [ ] Sourcemap strip/upload strategy, tied to error tracking setup
- [ ] Build the backend CD pipeline: Docker build, `drizzle-kit migrate` as an explicit pipeline step, deploy to Render (PROJECT.md §8) — not blocked by the AWS-account guardrail, can land independently of frontend CD
- [ ] Get Neon fully production-configured — currently just a project + a `local-dev` branch used for local dev (ADR 0010/0012), no production-readiness review done. Needs its own plan (Complex per `.claude/CLAUDE.md`'s classification), at minimum: a separate `production` branch distinct from `local-dev` (Neon's branching model, not reusing dev data); role/auth review (currently whatever the default Neon-created role grants — confirm least-privilege, not just "it works"); connection pooling limits appropriate for Render's expected concurrency (pooled-vs-direct string distinction already documented, but limits/timeouts not reviewed); backup/PITR retention policy (Neon's defaults vs. what this project actually needs); IP allowlisting or Neon's own network security options if available on the current plan tier; secrets handling for `DATABASE_URL` in Render's env vars (not `.env`) once deployed. Tied to — but not blocked by — the backend CD pipeline item above; do this before Render deploy, not after, so production isn't running against dev-grade config even temporarily.

## Observability

- [ ] Error tracking — decided 2026-08-14 (ADR 0014): Sentry's hosted free Developer tier, over self-hosting GlitchTip (adds a 4-container service to maintain for a project with no real users) or GlitchTip's own hosted free tier (viable, but loses the "transferable industry-standard practice" tiebreak — kept as the cheap fallback since it's SDK-compatible). Blocked on a real Sentry account + DSN before SDK wiring can start and be verified (same pattern as Neon/Render) — sign up, then wire `@sentry/react` (`apps/web`) and Sentry's Bun/Hono SDK (`apps/api`), verify a real captured error in the dashboard. Sourcemap strip/upload strategy stays separately deferred (tied to a deploy pipeline neither app has yet), not blocking basic error capture.

## Testing

- [x] **`packages/shared` has zero tests** — done 2026-08-13, alongside ADR 0013 (generating these schemas from Drizzle). `vitest.config.ts` (`node` environment) + `src/schemas/schemas.test.ts`, covering the happy path, nullability, and the two numeric-bound overrides (`Competition.tier`, `BallPosition.x`/`y`). Wired into root `bun run test`.
- [ ] Real E2E coverage in `packages/e2e/` beyond the one smoke test — add flow coverage once a real interactive feature exists (see ADR 0009), and a cross-package test once the frontend actually calls the backend
- [ ] No integration testing exists across any layer yet — CDK is `synth`-only, the API has a `curl` smoke test only, and `packages/db`'s schema is verified via `drizzle-kit generate` only (no live DB). Expected at this stage, not a bug. **Partially closed 2026-08-15: the frontend↔API seam is no longer untested** — `apps/web`'s index page now makes a real cross-origin request to `apps/api` (`docs/plans/2026-08-15-wire-frontend-to-api.md`). All three risks this item named for that seam were exercised for real, not assumed: CORS (browser request from `:5173` → `:4100` returned the correct `access-control-allow-origin`, no preflight failure, zero console errors), Zod/DB schema drift (response `safeParse`d against `packages/shared`'s `CompetitionSchema` at the JSON boundary — real payload matched exactly), and the down-API path (API killed mid-session, page degraded to a visible error rather than a blank screen or crash). Still open for the remaining boundaries: no automated cross-package test covers this seam (jsdom tests stub `fetch`; the real verification was manual), and CDK/live-DB/CD paths are untouched. Note `connect-src` is unrelated to what was verified here — it governs deployed CloudFront headers, not the Vite dev server, and stays deferred separately.
- [x] Test coverage reports with deltas as a CI PR comment — done 2026-08-14, new `.github/workflows/test-coverage.yml` using `davelosert/vitest-coverage-report-action` (273★, actively maintained, pinned to `8b157684c6a6b259b97d45e72b44242865c0f6a5` = `v2.12.2`). Chosen over Codecov after real research — Codecov's v5 action needs a token/OIDC signup for a repo's own pushes (tokenless uploads only work for fork PRs), an external-account cost this project already hit three times (Neon/Render/Sentry) and didn't need a fourth time for a project with zero real usage; `davelosert`'s action uses `GITHUB_TOKEN` only. `pull-requests: write` scoped to this workflow alone, same reasoning as `bundle-size.yml`/`dependency-review.yml` (`ci.yml` stays `contents: read`-only). Found and fixed a real gap along the way: only `apps/web` had `@vitest/coverage-v8` wired — added it to `apps/api`/`apps/ingestion`/`packages/shared` too (each needed `coverage.reporter: ["text", "json-summary", "json"]` and `reportOnFailure: true`), and expanded the root `test:coverage` script to chain all 4. The workflow deliberately runs each package's coverage as an independent `continue-on-error: true` step rather than the chained root script, so one package's test failure doesn't block the others' `coverage-summary.json` from being generated — verified locally that all 4 `coverage-summary.json` files are produced correctly (`bun run codegen && bun run test:coverage`). See `docs/plans/2026-08-14-coverage-pr-comments.md`. `threshold-icons: "{0: '🔴', 60: '🟠', 80: '🟢'}"` on each report step gives a visual bad/ok/good indicator per package without failing the build. Visibility only, no enforcement — the existing `coverage.thresholds` BACKLOG item below stays separate and deferred.

## Tooling / project setup

- [x] Bundle-size PR comments in CI — done 2026-08-13, new `.github/workflows/bundle-size.yml` using `preactjs/compressed-size-action` (verified via GitHub's API before adding: actively maintained, 654 stars, auto-detects Bun from the lockfile, default file pattern `**/dist/**/*.{js,mjs,cjs}` matches `apps/web/dist/` with no customization needed, reuses the existing `bun run build` script unmodified). Kept as its own workflow rather than a step in `ci.yml`, same reasoning as `release-please.yml` being separate — it needs `pull-requests: write` to post/update the comment, and `ci.yml`'s `contents: read` shouldn't be broadened just for one step. Pinned to the release commit SHA (`f322c295d...` = `v2.10.0`), YAML verified to actually parse.
- [x] Set up a workflow from GitHub's security/code-scanning template picker — done 2026-08-13, `.github/workflows/dependency-review.yml` using `actions/dependency-review-action` (verified via GitHub's API before adding: official `actions/` org, actively maintained, pushed the same day). **CodeQL was already active** (confirmed via the browser 2026-08-13: "Default setup", recent scan) so this fills the actual gap instead — diffs a PR's dependency changes specifically and fails on a newly-introduced known-vulnerable package, which `bun audit` doesn't do (that only checks the current lockfile state, not what a given PR changed). Configured `fail-on-severity: high` and `fail-on-scopes: runtime, development` to match `bun audit --audit-level=high`'s existing threshold rather than the action's own looser defaults (`low` severity, `runtime`-only scope). Kept as its own workflow, same `pull-requests: write` reasoning as `bundle-size.yml`. Pinned to the release commit SHA, YAML verified to parse.
- [x] Factored the 8 duplicated tsconfig strictness blocks into a shared `tsconfig.base.json` — done 2026-08-13, from an architecture review's second candidate. 7 files (`apps/api`, `apps/ingestion`, `packages/db`, `packages/e2e`, `packages/shared`, `infra`, `apps/web/tsconfig.app.json`) now `extends` it; `apps/web/tsconfig.node.json` left alone (shares almost nothing with the common block — extending it would cost more override lines than it'd save). `extends` under TS7's `tsc -b` composite build was spiked and verified first (no prior use of it anywhere in this repo), including confirming `infra`'s `exactOptionalPropertyTypes: false` override is genuinely load-bearing (reproduced the underlying `aws-cdk-lib` `Bucket`/`IBucket` type failure by temporarily removing it). No ADR — trivially reversible, doesn't clear that bar.
- [x] Enforce Conventional Commits PR titles — done 2026-08-14, new `.github/workflows/pr-title-lint.yml` using `amannn/action-semantic-pull-request` (1.4k★, industry-standard, pinned to `48f256284bd46cdaab1048c3721360e808335d50` = `v6.1.1`). No `actions/checkout` needed — reads the PR title via the API only. `pull_request` trigger (not `pull_request_target` — one contributor with write access, no fork PRs), `permissions: pull-requests: read` only (not opting into the `wip:`/error-comment features). No custom `types`/`scopes` config — defaults already match `commitlint.config.js`'s plain `@commitlint/config-conventional`, and `requireScope` defaults `false` so `release-please`'s own scopeless `chore: release main` title still passes. Not yet a required status check in the branch-protection ruleset — that's a manual GitHub settings step, same as `ci`'s original setup.

## Follow-up from adversarial review (2026-08-10)

- [ ] Set `connect-src` to the API's actual origin once it's deployed to Render — currently an explicit `'self'` placeholder (see ADR 0007/0008 cross-reference); will need updating again on the later AWS migration (ADR 0010)

- [ ] Add `@tanstack/eslint-plugin-query`/`@tanstack/eslint-plugin-router` (minimal ESLint alongside Biome, scoped to `apps/web` only — Biome has no equivalent) — **blocked, confirmed 2026-08-18, not just assumed**: `@typescript-eslint/parser` (required to parse `.ts`/`.tsx` at all, not only for the two plugins' type-aware rules) throws at import time under `typescript@^7.0.2`, on both stable and canary releases. Tracked upstream: [typescript-eslint#10940](https://github.com/typescript-eslint/typescript-eslint/issues/10940), open, no ETA. TypeScript's own "side-by-side with 6.0" workaround was attempted and didn't resolve cleanly under Bun (self-referencing alias install — see ADR 0003's update). Revisit once #10940 lands; when it does, the router plugin's rules (`create-route-property-order`, `route-param-names`) are pure-AST and need nothing but a working parser — no type-aware setup required for those two.

## Frontend housekeeping & conventions (raised 2026-08-17, after the Players screen)

Blocked on `feat/players-screen` merging, and the doc-touching items also blocked on `docs/sync-stale-docs` merging first (both touch `docs/design/frontend-ui-ux.md`/`frontend-architecture.md` — do this after, not in parallel, to avoid a third divergent copy).

- [x] New convention: no `void`-prefixed fire-and-forget promises (2026-08-17). `biome.json`'s `nursery.noFloatingPromises` requires one of `.then(ok,err)` / `.catch(err)` / `await` / `return` / `void` — no config option to add other accepted forms (verified against Biome 2.5.7 docs). Use `async` + `await` in the handler instead of `void`-ing the call. Converted all 5 `void navigate(...)` sites in `apps/web/src/routes/-players-page.tsx`, including the 4 passed directly as JSX callback props (`Select.onChange`, `Pagination.onChange`, `Button.onClick` ×2) — a standalone non-JSX repro suggested `nursery.noMisusedPromises` would flag an async function on a void-typed callback, but verified against the real Mantine-typed props that it doesn't: Biome's type inference doesn't trace through JSX prop assignment the same way it does a plain typed parameter. `bunx biome check`/`tsc -b` both pass clean. Documented (with the JSX-vs-plain-callback caveat) in `docs/conventions/typescript.md`. `rg "void [a-zA-Z]" -g '*.ts' -g '*.tsx'` repo-wide found no other occurrences.
- [x] New convention: `FC<Props>` (bare `FC` when no props) on every function component; `PropsWithChildren` when a component takes `children` (2026-08-17). Decided as a deliberate against-the-grain choice — React's own TS cheatsheet dropped this recommendation, but the reason (bare `FC` used to implicitly add `children`) no longer applies: `@types/react` 18 removed the implicit `children`. Verified empirically no conflict with `useExplicitReturnType`, `useNamingConvention`, `noNestedComponentDefinitions`, or `noReactPropAssignments` — a colon-typed variable declaration (`const Foo: FC<Props> = (props) => {...}`) satisfies all of them. Documented in `docs/conventions/react.md`. Converted every function component in `apps/web/src` — `IndexPage`, `CompetitionsPending`/`CompetitionsError`, `PlayersPage`, `PlayersPending`/`PlayersError`, `RootComponent`, `TestProviders` and its inline `wrapper`, and the two test-local `RoutedApp` helpers. Class components (`TestErrorBoundary`) untouched — this convention is function-component-only.
- [x] Fix the inline `meta` object type in `apps/web/src/routes/-players-route.test.tsx`'s `SamplePlayersResponse` (2026-08-17) — `docs/conventions/typescript.md`'s "Name inline object types" rule, flagged before. Exported `PlayerListMeta` (`PlayerListMetaSchema`'s inferred type) from `-queries/players.ts`; `SamplePlayersResponse.meta` now references it instead of hand-duplicating the shape.
- [x] Comment density pass — reduce to root `CLAUDE.md`'s existing "no comments unless the WHY is non-obvious" bar (2026-08-17). Originally scoped to `apps/web/src/routes/-players-page.tsx` and `-queries/players.ts`; widened twice at the user's direction to a full repo-wide pass — every package (`apps/web`, `apps/api`, `apps/ingestion`, `packages/db`, `packages/shared`, `packages/e2e`), `infra/`, and root-level CI config. Not a new rule, an enforcement pass — trims only, no logic changes. `packages/shared/src/schemas/*.gen.ts` deliberately untouched (gitignored, regenerated by `codegen`).
- [x] Extract the route-test render helper duplicated between `-index-route.test.tsx` (`renderRoute`) and `-players-route.test.tsx` (`renderPlayersRoute`) (2026-08-17) — new `src/test/render-route.tsx`'s `renderRouteTree(initialEntries)` (sibling to `render-with-providers.tsx`, same `createRouter`/`createMemoryHistory`/`TestProviders` wiring, parameterized on the only thing that differed). Also extracted `SAMPLE_PLAYERS`/`SAMPLE_COMPETITIONS`/`samplePlayersResponse` into new `src/test/route-fixtures.ts` per `docs/conventions/typescript.md`'s "Test fixtures don't live in a types file" rule — `SAMPLE_COMPETITIONS` had two independent inline copies (`-index-route.test.tsx`, `-index-page.test.tsx`), now one shared constant.
- [x] New convention: in Testing Library tests, destructure query functions from `render()`'s return value instead of the global `screen` object, and prefer an exact string over a regex in text matchers when an exact match works (2026-08-17). Documented in new `docs/conventions/testing.md`. Found a real edge case while retrofitting: `-index-page.tsx` renders `{competition.name} — {competition.country}` as one text node, so `getByText("Premier League")`'s default exact-match semantics don't match — use RTL's own `getByText("Premier League", { exact: false })` rather than reaching for a regex, documented as an explicit sub-case rather than an exception to the rule. Retrofitted all pre-existing `screen.`/regex usages across `-index-page.test.tsx`, `-index-route.test.tsx`, `-players-route.test.tsx`.
- [x] Adopt `wretch` as the API-client transport layer, hand-written not generated (2026-08-17) — resolves `docs/design/frontend-architecture.md` §9's open question. Investigated a much larger reference project's `m3ter-api` package for ideas; ported its `client/`+`util/`+`types/` layering closely (an `ApiClass` singleton with `configure()`, a `getClient()` request builder, verb function, plus a separate error type and path/param utils), deliberately skipped its `DataType`-enum/entity-map, declarative CRUD-config engine, and relationship-hydration machinery — all solve an ~80-resource-API problem this 4-endpoint project doesn't have. New `apps/web/src/lib/api/`: `api-error.ts` (`ApiError` class, `isApiError` guard), `api-client.ts` (`ApiClass`/`API`, `configure()`, `getClient()`, `get<T>()`), `types/requests.ts` (`Param`/`PathParams`/`QueryParams`), `utils/{error,params,path}.ts` (barrel-exported via `utils/index.ts`) — used by both `-queries/competitions.ts` and `-queries/players.ts`, which also dropped their hand-built `URLSearchParams` in favour of a `queryParams` object via wretch's `queryString` addon. Response validation via `packages/shared`'s Zod schemas is unchanged. `wretch` pinned exact (`"3.0.9"`, no `^`). Full rationale and empirical findings (wretch 3.x vs. the reference project's pinned 2.9.0 differ on error-body handling; `exactOptionalPropertyTypes`, on here and not there, needed the `Client` type's `ErrorType` generic pinned explicitly) in `docs/adr/0016-wretch-for-api-client.md`.
- [x] **Needs its own plan (Complex).** Generic state components — `-index-page-states.tsx` and `-players-page-states.tsx` are near-identical (pending/error, same retry-via-`router.invalidate()` pattern) and will keep duplicating per screen. Design a generic version before a third screen repeats it again. — done 2026-08-18: `src/components/common/GenericPending.tsx`/`GenericError.tsx`, parameterized by a `title` prop, same `useQueryErrorResetBoundary`/`router.invalidate()` retry behavior preserved. Both per-screen files deleted, routes wire the generic components with their own title. Landed alongside the generic `list()`/`listQueryOptions()` factory (see `docs/plans/2026-08-18-generic-list-query-factory.md`) — the `src/queries/` relocation those factories needed.
- [ ] **Needs its own plan (Complex), research not guesswork.** A `DataType` enum ↔ entity-interface mapping (e.g. `DataType.Player` → `Player`) — the user's stated motivation is feeding the generic-states work above (and plausibly future generic list/detail scaffolding) rather than a screen-specific need. Design what problem this actually solves and whether TypeScript can express the mapping soundly (discriminated unions vs. a lookup-type map) before writing code.
- [x] Unify every list endpoint's response shape to `{ data }` (`meta` added only where pagination needs it) — done 2026-08-18, alongside `apps/web`'s generic `list()`/`listQueryOptions()` factory hitting the bare-array-vs-envelope asymmetry between `/competitions` and `/players`. Reverses `PROJECT.md` §4's original "two shapes, deliberately" rule (envelope only for paginated routes) — `/api/v1/competitions` and `/api/v1/seasons` now also return `{ data }`, no `meta` key. Backend: `apps/api/src/routes/competitions.ts`/`seasons.ts` wrap their response and rename the OpenAPI schema (`CompetitionList` → `CompetitionListResponse`, `SeasonList` → `SeasonListResponse`); `players.ts`'s comment updated to match. Frontend: `CompetitionsList.tsx` unwraps `.data` same as `PlayersList.tsx` already did; test fixtures/mocks updated (`CompetitionsList.test.tsx`, `routes/-index.test.tsx`). Verified live against the real `local-dev` Neon branch via `run-api`'s `smoke.sh`, not just the mocked unit tests — both endpoints return the new envelope. `PROJECT.md` §4 updated in place (its own established correction-note convention, not a frozen ADR).
- [ ] **Needs its own plan (Complex), research not guesswork.** `apps/web/src/routes/` directory structure as the screen count grows (e.g. per-entity subdirectories). `docs/design/frontend-architecture.md` §2 already has a deferred-with-trigger table for exactly this (`src/features/<domain>/`, trigger: "3+ routes clustering on one domain entity, each with own queries and non-trivial components"). Frame this as "does the trigger fire yet" against the real route count, not a fresh design — `/players` alone doesn't meet it per that doc.
- [x] Adopt `mantine-react-table` (MRT) as the generic `DataTable` component, wire it into the Players list with server-side pagination/sorting/filtering — done 2026-08-19. Full history (library research vs. `mantine-datatable`, the pagination/sorting/filtering rollout, and a real MRT/Mantine-9 peer-dependency bug found and fixed) in `docs/plans/2026-08-18-mantine-datatable-generic-component.md`. Headline finding: `mantine-react-table` has never published a version supporting Mantine 8/9 (peer dep capped at `^7.9` across all 89 published versions, including our pinned `2.0.0-beta.9`) — its built-in column-filter row silently rendered collapsed because Mantine 9 renamed `Collapse`'s `in` prop to `expanded`. Fixed via `bun patch` (5 mechanical one-line renames across `dist/index.esm.mjs`/`index.cjs`, tracked as `patches/mantine-react-table@2.0.0-beta.9.patch`), not a fork — scoped precisely by cross-referencing Mantine's 7→8/8→9 migration guides against every file in MRT's source, which turned up exactly this one functional break and nothing else. Verified live against real DB data: filter row renders inline, typing filters correctly, combines with the position `select` filter, sorting/pagination unaffected.
- [x] Extract DataTable pagination/sorting/filtering handler logic into a reusable, entity-agnostic hook, then collapse it fully into `DataTable` itself — done 2026-08-19. `apps/web/src/hooks/useServerDataTable.ts` owns pagination (fully generic, `ListSearchSchema`'s `page`/`pageSize`) and sorting (generic via a new `sortableSearchShape(fieldEnum)` factory in `routing/search.ts`, parameterized per entity's sortable-field enum); filtering needs a small declarative `filterFields` map (columnId -> search param) but shares the normalization logic (empty string -> `undefined`, `MRT_Updater` resolution) — deliberately scoped to scalar text/select filters. After a follow-up request for deeper encapsulation, the hook plus the empty-state branching (three variants: genuinely empty / page-out-of-range / filtered-to-empty, generic via one `entityNamePlural` prop) moved fully inside `DataTable.tsx` — a consumer now just renders `<DataTable columns data meta search setSearch filterFields entityNamePlural ...mrtOptions />`, no hook import, no `EmptyState` JSX. `PlayersList.tsx` dropped from ~237 to ~80 lines. Found and fixed a real bug during live verification: calling `useMantineReactTable` unconditionally (required by Rules of Hooks) meant a genuinely out-of-range `?page=999` got silently corrected back to page 1 by Mantine's own `<Pagination>` self-correcting on mount before the "Page not found" state was ever seen — fixed by clamping only the `pageIndex` fed to MRT, independent of the unclamped values the empty-state messaging reads. 11 hook unit tests + 4 new `DataTable.test.tsx` tests + the existing 8 route-level tests all green, zero route-test changes needed. See `docs/plans/2026-08-19-server-data-table-hook.md`.
- [x] Move the players route off `useSuspenseQuery` so sort/filter/pagination changes don't fall back to a full-page Suspense reload — done 2026-08-19. `PlayersList.tsx`: `useQuery` + `placeholderData: keepPreviousData`, `isFetching` fed to a new `DataTable` `isFetching` prop wired to MRT's `state.showProgressBars` (thin bar in the bottom toolbar, rows stay visible — not `showSkeletons`, which would hide the very data `keepPreviousData` preserves; confirmed with the user rather than guessed). Route's `loader` (`routes/players/index.tsx`) dropped `loaderDeps` entirely — with it present, _every_ param change still re-runs `ensureQueryData` and re-creates the router match, so a slow request would still trip the router's own `pendingMs`-gated pending fallback even after the `useSuspenseQuery` fix; a constant `loaderDepsHash` keeps the match stable across search changes, so the loader now runs once per real route entry and all param reactivity is the component's own query. This is the opposite of the pattern in TkDodo's "Reliable Query Prefetching with TanStack Router" (linked mid-task, published the same day) — considered switching, kept this design; full reasoning for both decisions in the plan's follow-up section. Along the way, live-browser deep-link testing (`?page=3&sort=name`) surfaced a real pre-existing bug: `useServerDataTable`'s `onColumnFiltersChange` fired on mount (MRT's filter-input UI syncing its own state) and unconditionally reset `page` to 1 even when nothing had changed — fixed by skipping the `setSearch` call entirely when the resolved filter values already match `search`. New test in `-index.test.tsx` asserts rows stay mounted (no full-page pending swap) during a pagination change. Also deleted `apps/web/src/test/render-data-table.tsx`, an orphaned duplicate of the render helper already inlined in `DataTable.test.tsx` (zero importers, broken import path after an earlier directory restructure) that was blocking `bun run typecheck`. See `docs/plans/2026-08-19-players-in-table-loading.md`.
- [x] Make `DataTable` fit the viewport so the header/pagination footer stay in view instead of the whole page scrolling — done 2026-08-19. `enableStickyHeader`/`enableStickyFooter` were already set but never verified working after the MRT/Mantine-9 `Collapse` patch — turned out `enableStickyHeader` already worked internally (MRT computes its own `max-height: clamp(350px, calc(100vh - toolbarHeights), 9999px)`), but that calc is blind to page chrome _outside_ MRT (this page's `Title` + `Stack p="xl"`, plus the bottom pagination toolbar's own height), so the whole page still scrolled to reach pagination — confirmed live (a screenshot alone gave a false "looks pinned" read; scrolling programmatically to the pagination button revealed the title had scrolled out of view too). Fixed with one `mantineTableContainerProps.style.maxHeight` default in `DataTable.tsx` (`calc(100dvh - 12rem)`, not a flex-based rewrite — an advisor review flagged that a flex-chain approach risks _silent row clipping_ via `max-height: 100%` against a flex-sized parent, more surface area for the same result). The offset itself needed a second pass: first attempt only measured chrome _above_ the table (92px) and still left a 54px scroll gap — the bottom pagination toolbar's own 57px height (below the scrollable container, but still part of the page) wasn't accounted for. Also removed `enableStickyFooter` from `PlayersList` — confirmed no-op, it sticks a `<tfoot>` row and no column defines a `Footer`; misleading to leave in a change specifically about the footer staying visible when a _different_ prop (the bounded container) is what actually does that. Verified live: page no longer scrolls (`scrollHeight === innerHeight`), all 25 rows reachable via the inner scrollbar (no clipping), `?page=999` empty state unaffected. Short-viewport degradation reasoned through rather than empirically verified — this session's browser-automation `resize_window` didn't produce a real viewport change in this environment. See `docs/plans/2026-08-19-datatable-viewport-fit.md`.
- [x] Render `DataTable`'s empty state inline within the table body (keep header/pagination visible, replace only the rows) instead of swapping out the whole table — done 2026-08-19. Found MRT's built-in `renderEmptyRowsFallback` option (checked before building anything custom, per the task) — automatically rendered inside a single `<td colSpan={visibleColumns}>` whenever `rows.length === 0`, header/toolbars/pagination untouched. Moved the existing three-variant `EmptyState` JSX (genuinely empty / filtered-to-empty / page-out-of-range) into that callback; added `enablePagination: total > 0` (nothing to page through once there are zero results). Both added to `ManagedOptionKeys` so a consumer can't silently override them via the passthrough spread. Confirmed with the user: sorting/filtering stay always-active regardless of empty state — filters especially need to stay editable to escape a filtered-empty state, not just via "Clear filters". 5th `DataTable.test.tsx` test added asserting the column header stays visible during an empty state (uncheckable under the old branch, since the header didn't exist there at all) — all 5 pass. Verified live: filtered-empty search keeps filter inputs interactive with pagination gone; `?page=999` keeps pagination _present and usable_ (since `total > 0` there) alongside "Go to first page" — clicked it, confirmed correct navigation back to real data. See `docs/plans/2026-08-19-datatable-inline-empty-state.md`.
- [x] Add a table actions header for show/hide-filters and similar toggles — done 2026-08-19. `PlayersList` had `enableTopToolbar={false}` with no UI to hide the filter row once `initialState.showColumnFilters: true` was hardcoded on. Turned out MRT already ships a stock `MRT_ToggleFiltersButton` in its default top-toolbar button set — just needed `enableTopToolbar` back on, no custom button. Asked the user to scope which of MRT's other default buttons (density toggle, fullscreen toggle — both default to on) belonged here: kept all three rather than trimming to filter-toggle-only. Re-enabling the toolbar reintroduced the viewport-scroll issue task #9 had just fixed — its offset (`calc(100dvh - 12rem)`) was calibrated with the toolbar off, so its 56px (measured) reappeared as page scroll; revised to `calc(100dvh - 16rem)`, reconfirmed `scrollHeight === innerHeight`. Verified live: filter-toggle hides/shows the filter row correctly, density and fullscreen buttons present and clickable. Full pipeline green (40 web tests, up from 38 at session start). Follow-up noted in `docs/plans/2026-08-19-datatable-viewport-fit.md` rather than a new plan file — Trivial-classified (one-line prop removal + one offset value), but touched that plan's exact offset calculation.
- [x] Re-enable the per-column 3-dot actions menu (`enableColumnActions`) and make column widths consistent — done 2026-08-19. `enableColumnActions={false}` in `PlayersList` removed (default `true`); menu shows sort asc/desc/clear plus clear-filter/filter-by-column for filterable columns (pinning/hiding/grouping stay off, matching existing settings). Column-width inconsistency traced to `layoutMode: "semantic"` (MRT's default) — confirmed via docs that `grow`/`size` are no-ops in that mode, columns just size to content. Switched `DataTable` to `layoutMode: "grid"` with `defaultColumn: { grow: 1 }` (a `DataTable`-level default, not per-consumer, matching the established pattern — a future consumer can still override per-column or via its own `defaultColumn`). A third ask — a filter icon in the header — turned out to require `columnFilterDisplayMode: "popover"`, which would have replaced the just-added subheader filter row and toggle-filters button entirely (MRT hides the toggle button in popover mode since there'd be nothing left to toggle); asked and confirmed out of scope, keeping the current row + toggle button. Verified live: menu opens with correct items, all 4 columns measure equal width, page-2 click still navigates correctly under the new layout mode. Full pipeline green, all 40 web tests still pass.

# BACKLOG

- [ ] Backfill historical seasons' teams/fixtures — 2026-08-14's ingestion pass deliberately scoped to each competition's **current** season only (5 seasons, ~1,163 fixtures); the other 10 already-ingested seasons (`seasons.isCurrent = false`) have no fixtures/teams yet. Revisit once something actually reads historical fixtures (e.g. `/players/:id/stats?season=` for a past season) rather than backfilling speculatively now.
- [x] Ingest players/player_season_stats for the remaining competitions — done 2026-08-14. `apps/ingestion/src/index.ts`'s player-stats block generalized from Premier-League-only to a `PLAYER_STATS_COMPETITION_NAMES` loop (Premier League, Bundesliga, La Liga, Community Shield — "Club Friendlies 1" deliberately excluded, see the BACKLOG item). First run confirmed Premier League (idempotent re-verify) and Bundesliga fully, but hit a real `429` mid-La-Liga (Sportmonks `retry-after: 2840`s), which also exposed and fixed two real bugs: the per-player fetch loop had no failure isolation (a single 429 discarded every already-fetched player in that competition, not just the failed one) and the outer per-competition loop had none either (one competition hitting the rate-limit wall silently prevented every subsequent competition from being attempted, which is why Community Shield never even started). Both fixed with per-item/per-competition try/catch, matching this codebase's established isolation pattern elsewhere. Re-ran La Liga + Community Shield only once the rate-limit window reset: La Liga — 20 teams, 707/751 players, 725/769 stats (44 failures cascade from a handful of academy/reserve players Sportmonks has no full profile for, same root cause as Premier League's youth-squad gaps); Community Shield — 2 teams, 66/66 players, 66/66 stats, 0 failures. Verified directly against the real `local-dev` Neon branch (`SELECT` against `player_season_stats` scoped to each competition's finished season, not the current 2026/2027 one). Real evidence that rate-limit _handling_ (the separately-tracked TODO item, honoring `retry_after`/backoff) matters sooner than "someday," even though this fix itself deliberately stayed scoped to isolation, not full backoff.
- [x] Scan the existing codebase for violations of `docs/conventions/typescript.md`'s judgment-based rules and fix them — done 2026-08-14 across two follow-up passes: a full naming-convention scan (fixed clear abbreviations, left framework idioms like Hono's `c` and Playwright's `msg`/`err` as-is), then the remaining inline-object-type/inline-return/loop scan (extracted `IngestionFailure`, named both ingest results, kept the per-item-error-isolation loop). Also extracted the last duplicated `error instanceof Error ? ... : String(error)` expression into `apps/ingestion/src/to-error-message.ts`'s `toErrorMessage()`, and added a "Where types live" section to `typescript.md` covering the inline/co-located/`packages/shared` promotion tiers.
- [x] Give `apps/web/src/lib/query-client.ts`'s `QueryClient` a construction seam (`createQueryClient()` factory instead of a module-level singleton) — done 2026-08-15. This item's own stated trigger ("hypothetical until a component actually calls `useQuery`") fired exactly when `-index-page.tsx` became the first component to do so; bundled into that change rather than done speculatively. The module-level `queryClient` singleton stays for `__root.tsx`; the factory takes an optional `QueryClientConfig` so tests build an isolated client per test. That parameter is not speculative generality — TanStack Query v5 retries failed queries 3× with backoff by default, which would turn the error-state test into a timeout rather than a clean failure, so tests pass `retry: false`. See `docs/plans/2026-08-15-wire-frontend-to-api.md`.
- [ ] Watch for a shared entity-ingestion-pipeline abstraction in `apps/ingestion` (a generic `ingestEntity<T>()` over the fetch/normalize/orchestrate shape). **Partially resolved 2026-08-14 now that `seasons` is a second real entity**: the HTTP fetch/error-handling boilerplate genuinely was identical, extracted into `sportmonks-client.ts`'s shared `fetchSportmonks<T>()`. The orchestrator level (`ingest-competitions.ts`/`ingest-seasons.ts`) still isn't a clean generalization, though — `ingestSeasons` needs an extra DB pre-query to resolve `competitionId` via `league_id` that `ingestCompetitions` doesn't, and `normalizeSeason`/`normalizeCompetition` differ in arity (one vs. two params). Two data points made the fetch-level duplication real but showed the orchestrator-level shape is looser than it looked — forcing a shared abstraction now would still be guessing at what's invariant vs. incidental. Revisit at a third entity, or once `ball_positions`' batch-insert needs actually strain the current per-entity duplication.
- [ ] Generate Zod schemas for `squad_memberships`/`ingestion_runs` too (currently Drizzle-only, no JSON-boundary validation need today) — revisit if either ever needs one; see ADR 0013's Consequences
- [x] `packages/db` has no `.env.example` despite needing `DATABASE_URL` (direct) for `drizzle-kit generate`/`migrate` — done 2026-08-14, `packages/db/.env.example` (direct connection string, no `-pooler` suffix — distinct from `apps/api`/`apps/ingestion`'s pooled one, per ADR 0012).
- [ ] Stricter CSP via a CloudFront Function nonce, dropping `style-src 'unsafe-inline'` (currently needed for Mantine's runtime styles — see ADR 0007)
- [ ] Custom domain + ACM + Route53 for CloudFront (confirmed non-breaking to add later, just not needed yet)
- [ ] `bun audit` allowlist for a genuinely unfixable advisory — blocked on Bun tooling (no `.auditignore`-equivalent exists yet). **2026-08-14: the nanoid `<3.3.18` advisory that motivated this item turned out fixable**, not a real instance of the gap — `postcss`'s own `nanoid` range (`^3.3.17`) already permitted the patched `3.3.18`, just hadn't re-resolved; fixed via a root `overrides` field (`package.json`), no allowlist needed. Left open because the underlying gap is real: `overrides` only helps when a non-vulnerable version exists within every consumer's declared range (here, both `postcss`'s `^3.3.17` and `@scalar/types`'s `^5.1.6` happened to collapse safely onto `3.3.18` — verified via `apps/api`'s `/reference` smoke test, not assumed). A genuinely unfixable advisory (no compatible patched version anywhere in the tree) still has no escape hatch, and `--audit-level=high` would still block every PR indefinitely when that happens. Worth deciding whether `--audit-level=critical` is the safer default until the allowlist mechanism exists — a real severity-vs-availability trade-off, not something to change silently.
- [ ] Coverage thresholds (`coverage.thresholds`) once there's enough real code for a number to mean something
- [ ] API docs UI beyond Scalar (e.g. a generated typed client from the OpenAPI spec) — no consumer for one yet

# DONE

## Scaffold

- [x] Grilling session to decide the stack: Bun, single package, Vite + React SPA, TanStack Router, TypeScript 7 strictest, Biome, Vitest + RTL
- [x] ADRs 0001–0005 (SPA over meta-framework, Bun, TypeScript 7, Biome, foundation libraries: Zustand/TanStack Query/TanStack Form/Zod)
- [x] Automatic import ordering/grouping via Biome's `organizeImports`
- [x] lefthook pre-commit hooks + commitlint (Conventional Commits)

## Production readiness

- [x] CI (`.github/workflows/ci.yml`): lint, typecheck, test, build, `bun audit`
- [x] Accessibility testing via `axe-core` run directly in tests
- [x] Bundle size budget via `vite-plugin-bundlesize`
- [x] Dependabot for `bun` deps + GitHub Actions versions

## Styling migration

- [x] Replaced Tailwind v4 + shadcn/ui + Base UI with Mantine (ADR 0006)

## Tooling hardening (post-review)

- [x] Fixed the a11y test blind spot: assert `results.incomplete` empty too, not just `results.violations`; explicitly disabled `color-contrast` (jsdom has no layout engine) instead of leaving it silently unevaluated
- [x] Pinned `@biomejs/biome` exactly so nursery-rule drift lands with its own version-bump PR, not a random unrelated one
- [x] Removed `src/lib/env.ts` — validated only Vite's own guaranteed env fields, ~16kB gzip for zero real value
- [x] Removed the unenforceable `engines.node`/`.nvmrc` pin (confirmed Bun doesn't check it, and nothing in the toolchain shells out to Node anyway)
- [x] Wired up the previously-unused `@vitest/coverage-v8` (`test:coverage` script)
- [x] Added Bun install cache to CI (`~/.bun/install/cache`, keyed on `bun.lock`, per Bun maintainer guidance)

## AWS hosting infra

- [x] CDK stack: private S3 + CloudFront (OAC) + SPA fallback + security-headers/CSP policy (ADR 0007), verified via `cdk synth`

## Backend

- [x] Hono + REST/OpenAPI backend as a Bun workspace (`packages/api`), over tRPC/Express/Fastify/NestJS (ADR 0008)
- [x] `GET /health`, `/doc` (OpenAPI spec), `/reference` (Scalar UI) — spike only, verified via `app.request()`

## Documentation

- [x] ADR index (`docs/adr/README.md`)
- [x] `LICENSE` (proprietary/all-rights-reserved)
- [x] Prettier for Markdown only (Biome doesn't support it yet — confirmed against [biomejs/biome#3718](https://github.com/biomejs/biome/issues/3718))
- [x] `release-please` for automated per-package CHANGELOG/versioning from existing Conventional Commits
- [x] `SECURITY.md`, `CONTRIBUTING.md`
- [x] This file
- [x] Project-level `CLAUDE.md` — researched Claude Code's actual CLAUDE.md/monorepo docs first (not guessed), hand-written rather than `/init`-generated, kept under 200 lines by linking to README/CONTRIBUTING/ADRs instead of duplicating them
- [x] `.claude/skills/new-adr` and `.claude/skills/new-workspace-package` — codify `CONTRIBUTING.md`'s ADR-writing and workspace-package checklists as invocable skills, researched against Claude Code's actual skills docs (rules vs. skills vs. subagents, `.claude/commands/` now merged into skills) rather than guessed
- [x] `.claude/skills/run-console` and `packages/api/.claude/skills/run-api` via `/run-skill-generator` — both actually launched, driven, and verified (not paraphrased): the frontend originally via a bespoke Playwright screenshot driver (`chromium-cli` unavailable here, used the generator's own documented fallback), the backend via a `curl` smoke script. Found and documented a real gotcha in the process — two processes can both "successfully" listen on port 3000 (broad `*:3000` vs. loopback-only `[::1]:3000`) with no bind error, silently answering requests from the wrong one. Pulled the deferred Playwright dependency forward deliberately (confirmed with you first) rather than adding it silently.
- [x] `packages/e2e` as its own workspace package (ADR 0009) — moved off the root package.json, swapped the bare `playwright` library for the real `@playwright/test` runner, replaced the hand-rolled background-launch/poll/kill sequence with Playwright's own `webServer` config, rewrote `run-console` to point at the real suite instead of the bespoke driver (deleted). Verified passing via `bunx playwright test`, screenshot actually looked at, not just trusted.

## Football analytics — Phase 0 (2026-08-11)

- [x] Signed up for Render and Neon (ADR 0010) — no service/project configured yet, accounts only
- [x] Scaffolded `apps/ingestion` (empty deployable, `bun run dev`/`typecheck`) and `packages/shared` (empty library, `export {}` placeholder) as new Bun workspace members, per ADR 0011's `apps/` (deployables) vs `packages/` (shared libraries) convention and PROJECT.md §11 Phase 0. Wired into root `tsconfig.json` references, `release-please-config.json`/`.release-please-manifest.json` (both start at `0.0.0`), and README's Stack/Directory-structure/Commands sections. Verified via the full pipeline (lint, typecheck, test, build, `bun audit`).

## E2E in CI (2026-08-11)

- [x] Wired `packages/e2e` into CI — added a root `e2e` script (`bun run --filter ./packages/e2e test`, matching the existing `dev`/`build`/`test` delegation pattern) and two steps to `ci.yml` (`bunx playwright install --with-deps chromium`, then `bun run e2e`). Verified by actually running it locally the same way CI will (`CI=1 bun run e2e`), not just adding the YAML.

## Remote (2026-08-11)

- [x] Pushed to a real GitHub remote — `github.com/ben-m-hide/console`, over a dedicated personal SSH key + host alias (this is a work laptop otherwise signed into a company GitHub via `gh`/HTTPS); rewrote existing local commit authors from the work git identity to the personal one first, since that history predated the remote and hadn't been pushed anywhere yet. CI now runs for real on GitHub's runners instead of only being reproduced locally.

## Workspace restructure (2026-08-11)

- [x] Moved the frontend from the workspace root into `apps/web`, and `packages/api` into `apps/api` (ADR 0011, supersedes ADR 0008's original rejection of this) — new `apps/web/package.json`/`tsconfig.json`, root `package.json`/`tsconfig.json` slimmed to workspace-root/tooling/infra-deps only, root scripts delegate to `apps/web` via Bun's `--filter`. Fixed every concrete consequence found tracing the config graph: `.gitignore`'s path-anchored `routeTree.gen.ts` entry, `.vscode/settings.json`'s Mantine CSS lookup path, `release-please` manifest/config path rename plus a new `apps/web` entry, and moved `run-console` to `apps/web/.claude/skills/` to match `run-api`'s colocation. Verified via the full pipeline plus `packages/e2e`'s Playwright suite and `cdk synth`.

## Fixes from adversarial review (2026-08-11)

- [x] Fixed `.github/dependabot.yml` grouping every dev/production dependency together with no exclusions — meant an exactly-pinned package's bump (Biome, `aws-cdk-lib`, `hono`, `@playwright/test`, etc.) would land bundled into an unrelated weekly PR, defeating the entire reason it's pinned. Added `exclude-patterns` to both groups so every pinned package still gets its own individual PR. Verified the resulting YAML actually parses (installed `pyyaml`, loaded it), not just eyeballed the indentation.
- [x] Added an honest addendum to ADR 0009 acknowledging a fair "Simplicity first" tension raised by review (a third workspace package + formal ADR for one smoke test) — recorded as a real, deliberate bet rather than dismissed or silently reverted, since it was a discussed decision, not unrequested scaffolding.

## Fixes from adversarial review (2026-08-10)

- [x] Fixed `bun run build`/`typecheck` genuinely failing on a clean checkout — `tsc -b` ran before anything generated `src/routeTree.gen.ts` (gitignored, generated by the router plugin). Added a standalone `codegen` script (`tsr generate`, via `@tanstack/router-cli`) that both `build` and `typecheck` now run first. Reproduced the failure and the fix by actually deleting the file and re-running each script, not just reading the error.
- [x] Added an explicit `connect-src 'self'` to the CSP — was implicitly falling back to `default-src 'self'`, which would silently block the frontend calling the API the moment that's wired up. A recorded placeholder now, not an invisible gap.
- [x] Pinned every GitHub Action to a commit SHA (`actions/checkout`, `oven-sh/setup-bun`, `actions/cache`, `googleapis/release-please-action`) with the resolved version as a trailing comment — CI was the one place the project's own stated pinning policy (`CONTRIBUTING.md`) wasn't applied to itself. Added `permissions: contents: read` to `ci.yml` (least privilege; it had none before).
- [x] Removed the empty, untracked `src/hooks/`/`src/stores/` directories and corrected the README's directory diagram — git doesn't track empty dirs, so they wouldn't have existed on a fresh clone despite being listed.
- [x] Verified (didn't just accept) `setup-bun@v2`'s `packageManager`-field auto-detection claim against its own README — confirmed real, one reviewer sub-claim that didn't hold up.

## Football analytics — Phase 1 & 2 schema (2026-08-12)

- [x] Phase 1: Zod schemas + inferred types for all 8 entities in `packages/shared/src/schemas/`, barrel-exported (one file per entity, plus a `schemas/index.ts` barrel — see the barrel-export feedback note).
- [x] Phase 2: Drizzle schema for all 10 tables in a new `packages/db` workspace package (not in the original `PROJECT.md` plan — see ADR 0012 for why it's its own package and why `drizzle-orm/bun-sql` over `@neondatabase/serverless`), plus a generated initial migration (`packages/db/drizzle/0000_*.sql`, no live DB needed for `drizzle-kit generate`). Not yet run — no Neon project exists — and not yet wired into `apps/api`/`apps/ingestion`.
- [x] Per-directory `.claude/CLAUDE.md` for `apps/ingestion`, `packages/shared`, `packages/e2e`, `infra` (later followed by `packages/db`) — factual/current-state only, no invented skills.
- [x] Fixed the `bun.lock` trailing-comma warning in VS Code — first attempt (a `biome.json` `overrides` entry) turned out to be dead config: `biome check bun.lock` confirmed Biome hardcodes lockfiles as protected and never touches them at all, regardless of config. Reverted that, fixed the real source instead — switched `bun.lock`'s `.vscode/settings.json` file association from `jsonc` to `plaintext`, which has no validator attached at all.

## Review findings + GitHub repo settings (2026-08-12)

- [x] Verified 8 findings from an external review against actual repo state (not trusted at face value) — 4 confirmed and fixed: `squad_memberships` was missing its `sportmonks_id`/`UNIQUE` constraint entirely (a real upsert-idempotency bug, unlike `seasons`/`ball_positions` which just went beyond a paragraph's literal wording), `PROJECT.md` §4 had no documented rationale for the API being unauthenticated, `packages/shared` has zero test coverage, and Neon's pooled-vs-direct connection string distinction (confirmed against Neon's own docs) was undocumented in `packages/db`. Two more filed as open items (thin-vertical-slice-before-more-design note, `bun audit` severity-level trade-off); one (no branch protection on `main`, confirmed via `gh api` returning 404) flagged directly to the user rather than acted on, since it's a GitHub settings change.
- [x] Set up branch protection on `main` via GitHub's Rulesets (require PR, 0 required approvals for solo maintainer, require the `ci` status check, block force pushes, restrict deletions, empty bypass list) and disabled squash merging repo-wide (`Settings → General → Pull Requests`) in favor of rebase-only — squash was writing the PR title as the commit on `main` instead of the actual `commitlint`-validated message, which was silently breaking `release-please`'s Conventional Commits parsing since PR #16. Verified the fix worked end-to-end, not just configured: PR #23 (`release-please`'s own release PR) now shows a correctly parsed changelog entry sourced from a real commit message. Also verified via GitHub's own docs that a ruleset's own "Allowed merge methods" field (which still shows all three methods as its permissive default) can't override a stricter repo-level setting — the repo-level one is the actual gate.
- [x] Confirmed Dependabot alerts, Dependabot security updates, Secret Protection, and push protection were already enabled (checked directly in the browser, since this account's `gh` CLI auth isn't a repo collaborator and returns `null`/404 for anything admin-gated) — plus found CodeQL code scanning already active as a bonus, unprompted. Wiki tab already disabled. Nothing left to change here.
