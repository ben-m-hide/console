# Sportmonks API token, sent as the Authorization header (not ?api_token=).
SPORTMONKS_TOKEN=

# Neon's POOLED connection string (the "-pooler" hostname suffix), not the
# direct one — see packages/db's ADR 0012.
DATABASE_URL=
