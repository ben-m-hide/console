{
	"name": "@console-next/ingestion",
	"private": true,
	"version": "1.2.0",
	"type": "module",
	"scripts": {
		"dev": "bun run --watch src/index.ts",
		"typecheck": "tsc -b --noEmit",
		"test": "vitest run",
		"test:watch": "vitest",
		"test:coverage": "vitest run --coverage"
	},
	"dependencies": {
		"@console-next/db": "workspace:*",
		"@console-next/shared": "workspace:*",
		"drizzle-orm": "0.45.2"
	},
	"devDependencies": {
		"@types/bun": "^1.3.14",
		"@vitest/coverage-v8": "^4.1.10",
		"vitest": "^4.1.10"
	}
}
