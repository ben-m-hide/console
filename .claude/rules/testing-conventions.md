---
paths:
  - "**/*.test.tsx"
---

# Testing Conventions

See [`docs/conventions/testing.md`](../../docs/conventions/testing.md) — destructure queries from `render()` instead of `screen`, prefer exact strings over regex in matchers. Read it before writing or reviewing this file.
