---
paths:
  - "**/*.tsx"
---

# React Conventions

See [`docs/conventions/react.md`](../../docs/conventions/react.md) — `<Fragment>` over the `<>` shorthand, and memoizing values/functions by default with `useMemo`/`useCallback`. Read it before writing or reviewing this file. General TypeScript rules in [`typescript.md`](../../docs/conventions/typescript.md) apply here too.
